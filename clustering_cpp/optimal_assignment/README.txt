This folder contains the optimal cluster assignment vector found by the CG+DCA algorithm.
The CG+DCA algorithm saves the assignment vector in a file named <INSTANCE_NAME>_<K>.txt with 'n' rows (one for each data point) representing the cluster assigned to that point (from 0 to k-1)
